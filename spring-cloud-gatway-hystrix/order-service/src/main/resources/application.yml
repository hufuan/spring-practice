server:
  port: 9192

spring:
  #zipkin:
    #base-url: http://localhost:9411/
  h2:
    console:
      enabled: true
  application:
    name: ORDER-SERVICE
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    server-url:
      defaultZone: http://localhost:8761/euraka/
  instance: localhost

logging:
  file: C:/Users/fuhu/Desktop/order.log
